# Backend Local Development Environment Variables

# Server Configuration
PORT=8888
BASE_URL=http://localhost:8888
ENABLE_CORS=true
OUTPUT_DIR=./outputs
MAX_QUEUE_SIZE=10

# Database Configuration
DB_PATH=./data/generations.db

# Chrome Configuration (uncomment and set if Chrome is not in the default location)
# CHROME_PATH=/path/to/chrome

# Admin Authentication (change this for security in production)
ADMIN_TOKEN=admin

# Cleanup Configuration
CLEANUP_INTERVAL=24h  # How often to run cleanup (e.g., 24h, 12h, 1h)
RETENTION_PERIOD=168h # How long to keep files (e.g., 168h = 7 days)

# Sentry Configuration (optional)
# SENTRY_DSN=
# SENTRY_ENVIRONMENT=development
# SENTRY_RELEASE=og-generator-dev

# Logging level (debug, info, warn, error)
LOG_LEVEL=info

# Go Version
# This application requires Go 1.23 or higher

# Chrome Path (platform specific examples)
# Linux: CHROME_PATH=/usr/bin/google-chrome
# macOS: CHROME_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
# Windows: CHROME_PATH="C:\Program Files\Google\Chrome\Application\chrome.exe"

# Chrome Configuration
CHROME_PATH= 